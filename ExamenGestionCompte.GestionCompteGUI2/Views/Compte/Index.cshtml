@model IEnumerable<ExamenGestionCompte.Domaine.Entities.Compte>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DateOuverture)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Solde)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DateOuverture)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Solde)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RIB }) |
            @Html.ActionLink("Details", "Details", new { id=item.RIB }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RIB })
        </td>
    </tr>
}

</table>
